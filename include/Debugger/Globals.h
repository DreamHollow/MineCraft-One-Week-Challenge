#ifndef GLOBALS_H_INCLUDED
#define GLOBALS_H_INCLUDED 1

// Static CONST because unless you really need to change a static variable,
// it should remain in the same state for execution.

static const bool debug = true;
static const char db_string[8] = "DEBUG: ";

#endif